version: '3.8'

services:
  # All-in-one: Backend API + Frontend React app
  musicshare:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - audio_uploads:/app/uploads
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  audio_uploads:
